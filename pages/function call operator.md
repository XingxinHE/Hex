---
aliases: [call operator, application operator]
---
# 📝Definition
A function call is a kind of _`postfix-expression`_, formed by an expression that evaluates to a function or callable object followed by the function-call operator, **`()`**. An object can declare an `operator ()` function, which provides function call semantics for the object.
